export default function LocationDetailsSection({ id }) {
  return (
    <section id={id} className="section-shell bg-white">
      <div className="section-inner">
        <p className="section-kicker">Location details</p>
        <h2 className="section-title">Detalji lokacije</h2>
      </div>
    </section>
  );
}
