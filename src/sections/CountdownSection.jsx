export default function CountdownSection({ id }) {
  return (
    <section id={id} className="section-shell">
      <div className="section-inner">
        <p className="section-kicker">Countdown</p>
        <h2 className="section-title">Odbrojavanje</h2>
      </div>
    </section>
  );
}
