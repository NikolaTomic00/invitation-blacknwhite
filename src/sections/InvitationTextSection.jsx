export default function InvitationTextSection({ id }) {
  return (
    <section id={id} className="section-shell bg-white">
      <div className="section-inner">
        <p className="section-kicker">Invitation text</p>
        <h2 className="section-title">Tekst pozivnice</h2>
      </div>
    </section>
  );
}
