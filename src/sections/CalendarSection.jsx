export default function CalendarSection({ id }) {
  return (
    <section id={id} className="section-shell">
      <div className="section-inner">
        <p className="section-kicker">Calendar</p>
        <h2 className="section-title">Kalendar</h2>
      </div>
    </section>
  );
}
