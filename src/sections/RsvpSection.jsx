export default function RsvpSection({ id }) {
  return (
    <section id={id} className="section-shell bg-white">
      <div className="section-inner">
        <p className="section-kicker">RSVP section</p>
        <h2 className="section-title">RSVP</h2>
      </div>
    </section>
  );
}
